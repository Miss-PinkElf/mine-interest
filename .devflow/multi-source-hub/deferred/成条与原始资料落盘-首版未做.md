# Metadata（元数据）

- 创建时间（Created At）：2026-09-21 18:38:00 +08:00
- 更新时间（Updated At）：2026-09-22 18:35:00 +08:00
- 作者（Author）：Grok
- 目的（Purpose）：记录成条切片首版明确不做、后续再做的项。
- 关联仓库或项目（Related Repository / Project）：mine-interest-source-hub（`.`）。
- 关联任务（Related Mission）：`.devflow/multi-source-hub/`。
- 关联对齐（Related Align）：`plans/2026-09-21-多源信息中心-成条与原始资料落盘-align.md`。
- 当前状态（Status）：延期（Deferred），不是放弃。
- 文档边界（Scope / Boundary）：延期真相源。不触发实施。

# 成条与原始资料落盘：首版未做

## 首版已做

统一捕获封套、规则包（判断槽关闭直通）、QQ 单条/转发/会话成条、B 站按作品合并、`envelope.json` + `content.md`、共用 Vault、图片落盘、本地 Git、标记兼容空格与中英文标点、图片后台下载、已有 B 站档案回填。

## 明确延期

| 对象 | 原因 | 后续触发 |
| --- | --- | --- |
| 标签、摘要、维基等知识整理 | 先成条 | 统一条目稳定、用户要整理 |
| 判断槽真正执行 / 模型辅助判断 | 结构已留槽 | 用户要开判断时另写计划 |
| 日常群规则/正则/模型筛选 | 仍按 2026-09-09 延期 | 成条验收后填准入段 |
| 完整 14 项核心服务 | 先薄存储 | 条目契约稳定后再铺 |
| 自动 `git push`、Git LFS | 先本地、图先本机 | 用户建好私有库并确认无密钥 |
| 快照/媒体自动清理 | 本轮只定上限 | 与清理策略一起设计 |
| 机器人自身消息 | 仍等 NapCat 复测 | 重启后若仍不上报再选回填 |
| `content.md` 按转发层/每条消息分组标题 | 首版平铺图片链接 | 用户确认阅读层改法后做 |
| 会话内嵌套转发拆成独立 `qq/forward` 条目 | 首版按规则当会话子块 | 用户确认要拆再改规则 |
| 个别转发 `get_forward_msg` 返回空 | NapCat 对该 id 给空列表 | 无插件侧解法；可再转一次 |
| 已拆成单条的历史消息自动并回会话 | 当时标记未识别、队列堵塞 | 不回溯；重发段落即可 |
