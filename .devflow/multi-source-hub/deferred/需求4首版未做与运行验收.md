# Metadata（元数据）

- 创建时间（Created At）：2026-09-23 16:52:08 +08:00。
- 作者（Author）：Codex。
- 目的（Purpose）：区分需求 4 首版边界、明确延期项与尚待运行验收的事项。
- 关联仓库（Related Repository / Project）：`.`。
- 关联任务（Related Mission）：`.devflow/multi-source-hub/`。
- 原始需求（Original Requirement）：`zzz-prompt-debug/prompt-1.md` 第 34–38 行。
- 关联对齐（Related Align）：`plans/2026-09-23-QQ目录直连重试与安全自动推送-align.md`。
- 当前状态（Status）：延期与待验收（Deferred / Pending Verification），不是放弃。
- 文档边界（Scope / Boundary）：延期范围真相源（Source of Truth）；不授权自动实施。

# 需求 4 首版边界

| 对象或能力 | 本轮暂不做的原因 | 后续触发条件 / 推荐阶段 |
| --- | --- | --- |
| 显式代理配置（Proxy Configuration） | 本轮要求默认直连且不使用系统代理 | 用户需要特定网络环境或直连持续不可达时，重新对齐配置与凭据边界 |
| B 站持久媒体队列、断点续传与多分段视频 | 首版采用现有播放流接口返回的单段 MP4，下载发生在采集/补救流程；尚未设计大文件任务恢复 | 遇到分段作品、大文件阻塞轮询、重启续传需求或用户要求更高画质时进入媒体队列阶段 |
| 视频画质选择与完整性策略 | 首版使用现有接口参数，不保证最高画质或所有作品可取得 MP4 | 用户明确画质/格式目标并给出不适配样本后，单独对齐播放流策略 |
| 简介/评论链接筛选与格式化 | 与需求 4 下载及发布边界不同，尚未确认目标域名、展示格式与误匹配行为 | 用户确认规则和样本后进入独立 Align / Plan |
| 媒体上传远端、Git LFS | 用户本轮选择只发布 Markdown、索引及非敏感元数据，媒体保留本机 | 用户明确远端备份媒体的隐私、体积、保留策略后再讨论 |

## 待验收，不属于延期实施

- 首次真实远端推送（First Live Push）及后续自动推送：代码和合成测试已具备，目标私有性与 SSH 读取已核验；AstrBot 未运行，尚无真实推送证据。用户启动/重载插件后先检查远端文件清单与内容边界。
- 新 QQ 消息与新 B 站 @ 的端到端验收（End-to-End Verification）：插件已同步，但需用户发测试消息并核对目录、媒体与日志。
