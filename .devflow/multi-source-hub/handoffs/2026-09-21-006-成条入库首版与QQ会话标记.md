# Metadata（元数据）

- 创建时间（Created At）：2026-09-21 18:38:00 +08:00
- 更新时间（Updated At）：2026-09-21 18:38:00 +08:00
- 作者（Author）：Grok
- 目的（Purpose）：交接成条与原始资料落盘首版，供新对话恢复。
- 关联仓库或项目（Related Repository / Project）：mine-interest-source-hub（`.`）。
- 关联任务（Related Mission）：`.devflow/multi-source-hub/`。
- 关联对齐（Related Align）：`plans/2026-09-21-多源信息中心-成条与原始资料落盘-align.md`。
- 关联计划（Related Plan）：`plans/2026-09-21-多源信息中心-成条与原始资料落盘-plan.md`。
- 当前状态（Status）：有效（Active）。
- 文档边界（Scope / Boundary）：本切片交接。不替代 state.md。不含 Cookie、快照正文、私人聊天。

# 成条入库首版交接

## 当前目标

把 QQ 收藏群消息和 B 站 @ 收成统一资料条目（Item），每条一份 `envelope.json` 和一份可读 `content.md`。知识整理（标签/摘要/维基）不做。

## 当前阶段

成条切片已实施并通过离线测试；B 站 Vault 回填已做；QQ 会话标记已放宽。外层 14 项仍未开工。

## 当前进度

- 入库库：`backend/src/sourcehub/`，安装时同步进两个 AstrBot 插件的 `sourcehub/`。
- QQ 检查插件 0.5.0：快照 + 成条；群 `836229427`。
- B 站插件 0.2.0：旧档案仍写，同时按作品合并进 Vault。
- Vault：`~/.astrbot/data/sourcehub/`（相对 AstrBot cwd `~/.astrbot` 的 `data/sourcehub`）。
- 离线：`PYTHONPATH=.:backend/src backend/.venv/bin/python -m unittest discover -s tests -p 'test_*.py' -v`（收尾时 65 项）。

## 本轮完成内容

1. Align / Plan：先成条；判断槽预留关闭；B 站按作品合并；json+md 两层；独立私有 Git（本轮本地提交、不自动推）；图片默认落地、视频只留链接、50 MiB。
2. 实现入库库与两插件接入。相对导入修复 AstrBot `No module named 'sourcehub'`。
3. 真实验收：B 站用户认为没有大问题；专栏/动态/多次 @ 经 `export_existing` 回填进 Vault。
4. QQ：文字、单图、会话表情包合集（`qq:session:1693473199`，159 图）已落盘。
5. 修复：同步下图卡住事件循环约 10 分钟；complete 档案不导出；会话标记过严（不认空格/英文标点）。

## 关键决策与原因

- 先成条，再知识整理；判断槽首期关闭。
- 会话标记默认三个中文逗号/句号，后放宽为去空白 + 中英文等价。
- B 站无会话标记，按作品 ID 合并多次 @。
- 会话里的嵌套转发当子块，不另开 `qq/forward`（用户在 `，，，`…`。。。` 中间发的那份 35 条转发因此进了会话条目）。
- `media/` 是公共哈希库，不是条目。

## 关键文件

- `backend/src/sourcehub/`
- `integrations/astrbot/astrbot_plugin_sourcehub_inspector/`（含 `collect.py`）
- `integrations/astrbot/astrbot_plugin_sourcehub_bilibili/store.py`、`main.py`
- `tests/test_sourcehub_*.py`
- `deferred/成条与原始资料落盘-首版未做.md`

## 风险 / 阻塞 / 开放问题

- 个别转发 NapCat 返回空列表，条目几乎无正文。
- `content.md` 把转发内层图平铺，看起来不像「一条合集」。
- 第二段段落（段落测试/123/喜欢你）因标记未识别和排队，拆成了单条；历史不自动合并，需重发。
- 自身消息、自动清理、自动 push、知识整理仍延期。
- 改插件后必须覆盖 `~/.astrbot/data/plugins/` 并重载。

## 立即下一步

1. 重载两个 SourceHub 插件。
2. 用 `, , ,` 或 `，，，` 重发一段文字会话并 `。 。 。` / `。。。` 收口，确认一条 `qq/session/`。
3. 用户若要阅读层分组或会话内转发拆条，先对齐再改。

## 恢复指引

先读 `state.md`、`checkpoints.md`，再读本文件。不要重做 B 站只读采集方向。不要提交 Cookie、真实快照、`sourcehub-image-urls.json`。
