# Metadata（元数据）

- 更新时间（Updated At）：2026-09-09 18:55:33 +08:00
- 作者（Author）：Codex。
- 目的（Purpose）：恢复插件诊断上下文。
- 关联仓库（Related Repository）：mine-interest-source-hub（`.`）。
- 关联任务（Related Mission）：`.devflow/multi-source-hub/`。
- 原始需求（Original Requirement）：`zzz-prompt-debug/多种信息源整合.md`。
- 当前状态（Status）：交接中（Handoff）。
- 文档边界（Scope / Boundary）：本任务记录；诊断插件不代表完整信息中心已获准实施。

# 交接记录（Handoff）002

## 本轮变化与授权

- 001 的“没有代码、没有接入”已过时。用户先了解安装/适配器/插件，自己运行了桌面版 v4.27.5 并接入 QQ 官方机器人。
- 读取官方 https://docs.astrbot.app/dev/star/plugin-new.html 确认 metadata.yaml、data/plugins 放置、WebUI 重载及 data 下持久化要求。桌面版可开发插件，不要求再用 uv 安装。
- 用户确认先做事件检查、本地 JSON 和带前缀日志，实际创建了 integrations/astrbot/astrbot_plugin_sourcehub_inspector/；核心存储服务未创建。
- 初始手动复制后 ZIP 安装发生同名目录冲突，按用户授权删除旧插件目录后上传成功。之后用户明确偏好：仓库修改 → 覆盖已安装 main.py → WebUI 重载，不要重复 ZIP 安装导致冲突。
- 独立 logging 记录器没有进入 AstrBot 面板；改为基类 self.logger，三处保留 [SourceHub Inspector]。已核对安装文件与源码一致；最新五份快照证明处理函数运行，未独立取得修复后面板截图。
- 本轮最初创建插件没有先落盘计划，违反门禁；补事实文档不能追认当时已遵守。后续必须先计划再改。

## 真实测试证据（Local Evidence）

以下文件名相对 AstrBot 用户目录 data/sourcehub-inspector/snapshots/，不提交快照正文：

| 时间（北京时间） | 文件名前缀 | 实测 |
| --- | --- | --- |
| 18:46:25 | 20260909T104625589101Z | 普通文本“测试”存在 |
| 18:46:37 | 20260909T104637033496Z | 表情包有 Image 节点，指向 data/temp 的 GIF，核验时文件不存在 |
| 18:46:46 | 20260909T104646467294Z | 图片有 Image 节点，指向 data/temp 的 JPG，核验时文件不存在 |
| 18:46:53 | 20260909T104653694491Z | 一层转发为 Plain 展示文本，共 5 条消息，含一项图片说明/URL，没有独立 Image 节点 |
| 18:47:28 | 20260909T104728299141Z | 多层样例为 Plain 展示文本，共 20 条外层消息，第 1 条只有发送者；没有可见的内部转发树 |

消息对象这部分没有出现 <max-depth>，但不能据此证明采集完整：raw_message 是 PatchedGroupMessage 的字符串表示，没有结构化读取其 raw_data；整个事件包含 bot/platform 等不必要对象，其他位置仍可能截断。没有实际点击原始 QQ 记录对照，用户说客户端可见深层内容，应继续调查。

## 下一步（Next Actions）

1. 只读检查本机 v4.27.5 的 qqofficial 适配器、PatchedGroupMessage 和使用的 SDK；先确定哪些处理把合并记录变成 Plain，raw_data 是否保留嵌套标识，是否存在后续获取接口。
2. 不先假定是平台限制，也不通过大语言模型（LLM）编造原文。给出根因证据后最小计划，再改检查器并覆盖桌面版。
3. 检查器改进需处理真实 raw_data/slots/模型字段及循环引用；不要仅把深度阈值调大、递归整个 event 或字符串扫描充当完整节点。
4. 持久保存媒体需在临时文件存在时复制；文本里的转发附件 URL 需核验下载能力。离线打开本地文件才算保存通过。
5. 先收敛采集范围和脱敏，避免保存整个机器人对象；快照包含临时鉴权字段，不分享/提交现有原始 JSON。
6. 使用带唯一内外层文本标记和已知图片的样例验证各层；记录无法获取字段，不把外层条数当嵌套深度。

## 首版边界、未讨论与延期

- 当前只是诊断版本：JSON 保存、带前缀日志和手动热重载；没有下载器、持久媒体、正确事件去重、来源白名单、完整异常隔离或正式服务。
- event_id 当前读 event.message_id，可能回退时间；真实身份在 message_obj.message_id，尚未修复。每次接收新快照，没有此前口头说的重复标记功能。
- _safe_value 有深度 8 硬编码和 str 兜底；写盘同步且缺错误处理。已知问题保留，收尾不悄悄重构。
- 一个正式通用桥接插件复用平台适配器的方向仍成立；诊断插件将来保留独立还是合并为调试模块尚未决定，不承诺永久维护两个插件。
- 首个里程碑建议先采集、保存、查询、去重，后补智能整理；用户同意先按推荐讨论，但具体新任务顺序尚未正式替换完整计划。
- 未定：技术栈/隔离/存储恢复、后台/自动开关积压、模型服务/送模范围、重复转发身份、Git 媒体备份。
- 必须后续继续的当前缺口：嵌套转发获取、图片持久化、脱敏/事件边界；原因是先探测真实消息，本次用户要求交接；恢复诊断阶段进入，不能写成永久放弃或用户批准延期。
- 唯一已明确延期的内容能力仍为日常群规则/正则/LLM 筛选，首期闭环验收后再对齐；见 deferred/QQ指定群持续收集与规则筛选.md。
- GitHub 私人收藏同步、其他平台启用、历史导入、手记/UI 留 backlog，未经验证不能说完成。此次代码 push 不等于授权私人数据同步。

## 提交与验证边界

- 用户明确授权本次相关代码/文档提交并推送；不包含 .vscode/controlled-explorer.json、config.ts、.gitignore、tsconfig.json、根 devflow-handoff.md、真实聊天快照或凭证。
- 更新 ZIP 与仓库源码一致；本机安装源码已一致。只有用户真实事件实测和静态/归档核验，无完整业务自动测试；backend/.venv 尚未创建，不使用全局 Python 冒充验证。
- 提交和远端结果以 Git 日志/远端分支核验为准，不在提交前伪填成功哈希。
