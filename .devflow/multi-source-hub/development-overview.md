# Metadata（元数据）

- 创建时间（Created At）：2026-09-09 15:37:05 +08:00
- 作者（Author）：Codex
- 目的（Purpose）：保留本轮需求演进、取舍与调研纠正，供深度恢复
- 关联仓库或项目（Related Repository / Project）：mine-interest-source-hub（`.`）。
- 关联任务（Related Mission）：`.devflow/multi-source-hub/`。
- 原始需求（Original Requirement）：`zzz-prompt-debug/多种信息源整合.md`。
- 关联计划（Related Plan）：`.devflow/multi-source-hub/plans/2026-09-09-多源信息中心-通用接入与QQ收藏闭环-plan.md`。
- 当前状态（Status）：文档阶段收束，任务暂停（Paused）。
- 文档边界（Scope / Boundary）：整体过程记录，不是默认恢复热路径；决定以 decision-log.md 及当前状态为准。

# 多源信息中心开发总记录

## 本轮演进

1. 用户指定原始多源信息笔记，要求走重型开发流程（devflow）。
2. 发现已有终态 PRD、LLM Wiki 讨论和结构图；它们作为需求输入，不直接视为批准计划。先读局部，用户要求后完整阅读。
3. 编辑规则纠正为：原始数据保留且仅用户可编辑；二次内容用户与 AI 均可编辑。撤回人只读模型页面的推荐。
4. 明确本地 Git 历史、默认自动整理及开关、手动整理、独立后台及开关、外部工具与去重。
5. 调研机器人框架：用户允许本机 Grok 辅助，主代理用代理和 gh 核验公开文档。NoneBot2 未被选定；比较后用户选择借用 AstrBot。
6. 用户澄清多平台由 AstrBot 承接，信息中心只需一个桥接插件的统一数据，不另造平台连接框架。
7. 首期主动收藏，入口由短暂确认的私聊更正为两三个自用账号组成的现有群，用户自己为群主，转发即收藏无需 @/命令。
8. 日常群自动筛选明确延期，后续规则、正则和 LLM 辅助判断；不是把所有群消息接入延期。
9. 整体对齐稿获确认，计划已写并收口，proposal/design/tasks 已产出待审；用户当前决定暂停，不进入 Apply。

## 产品决定和工程建议必须分开

已批准的是上述产品方向与首期使用习惯。Python/FastAPI/SQLite、媒体分离、权限部署、租约、开关恢复积压策略、页面类型等是待审规格中的工程建议，不能因写进 design 就说用户已逐项批准。

规格 14 项任务仅是后续执行清单；无业务应用、无 AstrBot 安装、无 QQ 账号配置、无真实模型联调、无数据远端推送。前述“检查通过”仅指文档及引用检查。

## 调研证据与纠正

- 用户代理 127.0.0.1:7897 配合受审查沙箱外只读查询有效，gh 认证成功。沙箱内连接失败不能证明代理停机或 token 失效。
- PATH 未找到 Chrome 命令不能证明浏览器未安装。首次修改 HOME 和临时 Grok 目录尝试不是推荐操作，不在下次重复。
- Grok 公开调研输出由主代理交叉验证；本轮不含私人聊天或源代码上传给 Grok。
- 官方 QQ 群全量事件已文档化，AstrBot 当前源码包含处理；用户账号是否实际可用、转发和附件完整性仍需联调。
- go-cqhttp README 声明无力维护；Lagrange 当前 V2 指向 Milky；NapCat 混合许可，不能只看更新日期或模型记忆。
- AstrBot 标准 timestamp 可能是处理时间，需保留源时间；MCP 是客户端方向；插件/会话/模型开关不可混同；只解析首个引用不能证明合并记录完整。

原始核验来源和提交标识在 plans/ 下两份框架调研报告，不需要再次全量联网，除非版本或问题发生变化。

## 后续路线

默认先读暂停状态，再按用户意愿讨论或审阅规格。需要调整范围时更新 Align/Plan/Spec，不复读所有历史。日常群筛选已延期；GitHub 数据同步、文档拉取、旧消息导入和其他界面仍待单独安排，不冒充已批准实施。

## 文档更新判断

本轮建立任务记录、计划、规格、待办、明确延期、交接和恢复提示；给三份历史说明加当前状态提示，不改写全部历史内容。无软件调试实例需要新建 bug-log；环境诊断纠正已记录在调研和本文件，后续不要将未复现的环境失败当永久阻塞。
