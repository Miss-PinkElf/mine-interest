# Metadata（元数据）

- 更新时间（Updated At）：2026-09-09 18:55:33 +08:00
- 作者（Author）：Codex。
- 目的（Purpose）：记录当前工作路径。
- 关联仓库（Related Repository）：mine-interest-source-hub（`.`）。
- 关联任务（Related Mission）：`.devflow/multi-source-hub/`。
- 原始需求（Original Requirement）：`zzz-prompt-debug/多种信息源整合.md`。
- 当前状态（Status）：交接中（Handoff）。
- 文档边界（Scope / Boundary）：本任务记录；诊断插件不代表完整信息中心已获准实施。

# 当前工作流（Workflow）

- 主线：整体对齐与计划已有，完整规格待审；14 项业务任务未完成。
- 当前分支：用户已授权检查插件开发和本地验证，进入问题诊断（Debugging），暂停交接。
- 恢复：state.md → checkpoints.md → 最新 handoff → 按需查本机适配器实现。
- 先查原始消息和嵌套记录标识，再区分漏读字段、额外请求和上游缺失；不能重复询问 AstrBot/QQ 是否已接入。
- 工程顺序：对齐（Align）→计划（Plan）→任务（Tasks）→实施（Apply）→验证（Verify）。此前检查插件初建漏落盘计划，是流程偏差；本次仅补事实记录，不伪造前置完成。
- 修改源码后按用户偏好覆盖本机已安装插件，然后 WebUI 热重载；安装目录不是仓库。
- 根 devflow-handoff.md 只读。此次用户已授权提交并推送；不自动上传私人消息或开启产品同步。
